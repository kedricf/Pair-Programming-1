function vowelChecker(x){
    var vowels = x.toLowerCase();
     if (vowles  === "a" || vowels  === "e" || vowel === "i" || vowles === "o" || vowles  === "u") {

        console.log(" the  letter is a vowel!");
     }
     else {
     
      console.log(" letter is a NOT a vowel.");
     }
    }

   
        